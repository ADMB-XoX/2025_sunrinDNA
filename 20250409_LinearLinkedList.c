#include <stdio.h>
#include <stdlib.h>

struct node{
  int data;
  struct node* next;
}

void push(struct node** head, int data){
  struct node* temp=(struct node*)malloc(sizeof(struct node));

}

int main(){


}